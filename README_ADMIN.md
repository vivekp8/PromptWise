# Admin Portal Guide

## Access
- URL: `/admin/login`
- Default Credentials:
    - Username: `admin`
    - Password: `adminpassword123`

## Features
- **Dashboard**: System overview.
- **Audit Logs**: View security and system events.

## Development
- Frontend code is in `src/pages/admin/` and `src/components/layouts/`.
- Backend routes are in `api.py` under `# ✅ AUTHENTICATION ROUTES` (and near end of file).
